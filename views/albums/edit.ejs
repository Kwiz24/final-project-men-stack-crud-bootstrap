<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= album.albumName %></title>
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <h1>Edit <%= album.albumName %> by <%= album.artist %></h1>
    <form action="/albums/<%= album._id %>?_method=PUT" method="POST">
        <label for="artist">Artist:</label>
        <input type="text" name="artist" id="artist" value="<%= album.artist %>" required />

        <label for="albumName">Album Name:</label>
        <input type="text" name="albumName" id="albumName" value="<%= album.albumName %>" required />

        <label for="genre">Genre:</label>
        <input type="text" name="genre" id="genre" value="<%= album.genre %>" required />

        <label for="year">Year:</label>
        <input type="number" name="year" id="year" value="<%= album.year %>" required />

        <label for="artistImageUrl">Artist Image URL:</label>
        <input type="text" name="artistImageUrl" id="artistImageUrl" value="<%= album.artistImageUrl %>" />

        <label for="albumImageUrl">Album Image URL:</label>
        <input type="text" name="albumImageUrl" id="albumImageUrl" value="<%= album.albumImageUrl %>" />

        <button type="submit">Update Album</button>
    </form>
    <a href="/albums">Back to Albums</a>
</body>
</html>
