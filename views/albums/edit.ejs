<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Album</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Edit Album</h1>
    <div class="form-container">
      <form action="/albums/<%= album._id %>?_method=PUT" method="POST">
        <div class="mb-3">
          <label for="albumName" class="form-label">Album Name:</label>
          <input type="text" name="album[albumName]" id="albumName" class="form-control" value="<%= album.albumName %>" required>
        </div>
        <div class="mb-3">
          <label for="artist" class="form-label">Artist:</label>
          <input type="text" name="album[artist]" id="artist" class="form-control" value="<%= album.artist %>" required>
        </div>
        <div class="mb-3">
          <label for="genre" class="form-label">Genre:</label>
          <input type="text" name="album[genre]" id="genre" class="form-control" value="<%= album.genre %>" required>
        </div>
        <div class="mb-3">
          <label for="year" class="form-label">Year:</label>
          <input type="number" name="album[year]" id="year" class="form-control" value="<%= album.year %>" required>
        </div>
        <div class="mb-3">
          <label for="albumImageUrl" class="form-label">Album Image URL:</label>
          <input type="url" name="album[albumImageUrl]" id="albumImageUrl" class="form-control" value="<%= album.albumImageUrl %>" required>
        </div>
        <div class="mb-3">
          <label for="artistImageUrl" class="form-label">Artist Image URL:</label>
          <input type="url" name="album[artistImageUrl]" id="artistImageUrl" class="form-control" value="<%= album.artistImageUrl %>" required>
        </div>
        <button type="submit" class="btn btn-custom">Update Album</button>
      </form>
      <a href="/albums" class="btn btn-secondary mt-3">Back to Albums</a>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
