<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= album.albumName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4"><%= album.albumName %></h1>
    <div class="text-center mb-4">
      <img src="<%= album.albumImageUrl %>" alt="<%= album.albumName %> cover" class="img-fluid mb-2">
      <h2>Artist: <%= album.artist %></h2>
      <p>Genre: <%= album.genre %></p>
      <p>Year: <%= album.year %></p>
      <img src="<%= album.artistImageUrl %>" alt="<%= album.artist %>" class="img-fluid" width="200">
    </div>
    <div class="text-center">
      <a href="/albums/<%= album._id %>/edit" class="btn btn-warning">Edit</a>
      <form action="/albums/<%= album._id %>?_method=DELETE" method="POST" class="d-inline">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
      <a href="/albums" class="btn btn-secondary">Back to Albums</a>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
